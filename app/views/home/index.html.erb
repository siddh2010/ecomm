<h1>Welcome to the app</h1>
2 + 2 = <%= 2 + 2 %> <!-- embedded ruby code. With '=' will display the result on the page. So like a puts -->
<% names = ['sid', 'calum', 'bhavna', 'aman'] %> <!-- Not going do display the names array, cuz no equal to -->
<h2>Listing Categories...</h2>
<ol>
	<% @categories.each do |category| %>
		<li> <%=category.name%> </li>
	<%end%>
</ol>
<a href='/home/about'>About </a> <!-- This is not the way links should be done normally. We need to use ruby link_to -->
<%= link_to "About (generated by rails)", home_about_path%> 